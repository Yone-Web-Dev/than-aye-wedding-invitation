# Copy to .env.local for local dev; set same vars in your host dashboard for production.

# Optional — embedded map uses Plus Code if omitted
VITE_GOOGLE_MAPS_API_KEY=

# Add after you deploy (see README) — site works without these (demo mode for wishes/RSVP)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
